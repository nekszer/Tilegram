<Page
    x:Class="Tilegram.Feature.Profile.ProfilePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Tilegram.Feature.Profile"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:Local="using:Tilegram.Feature.Profile"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <!-- El DataContext de la página debe ser tu ViewModel -->
    <Page.DataContext>
        <Local:ProfileViewModel />
    </Page.DataContext>

    <Grid>

        <Hub Header="Perfil de Instagram">
            <!-- Sección de perfil -->
            <HubSection Header="Perfil">
                <DataTemplate>
                    <StackPanel Margin="20">
                        <!-- Foto de perfil -->
                        <Ellipse Width="100" Height="100" Stroke="Gray" StrokeThickness="2" HorizontalAlignment="Center">
                            <Ellipse.Fill>
                                <ImageBrush ImageSource="{Binding Profile.Picture}" />
                            </Ellipse.Fill>
                        </Ellipse>
                        <!-- Nombre de usuario y nombre completo -->
                        <TextBlock Text="{Binding Profile.UserName}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                        <TextBlock Text="{Binding Profile.FullName}" FontSize="16" Foreground="Gray" HorizontalAlignment="Center"/>
                        <!-- Biografía -->
                        <TextBlock Text="{Binding Profile.Description}" TextWrapping="Wrap" FontSize="14" Margin="0,10,0,0" HorizontalAlignment="Center" />
                        <!-- Estadísticas -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                            <StackPanel Margin="20">
                                <TextBlock Text="{Binding Profile.PostCount}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
                                <TextBlock Text="Publicaciones" FontSize="12" HorizontalAlignment="Center"/>
                            </StackPanel>
                            <StackPanel Margin="20">
                                <TextBlock Text="{Binding Profile.FollowerCount}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
                                <TextBlock Text="Seguidores" FontSize="12" HorizontalAlignment="Center"/>
                            </StackPanel>
                            <StackPanel Margin="20">
                                <TextBlock Text="{Binding Profile.FollowingCount}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
                                <TextBlock Text="Seguidos" FontSize="12" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- Highlights -->
                        <TextBlock Text="Highlights" FontSize="18" FontWeight="SemiBold" Margin="0,30,0,10" HorizontalAlignment="Center"/>
                        <ScrollViewer VerticalScrollMode="Auto" HorizontalAlignment="Center" Visibility="Collapsed">
                            <GridView ItemsSource="{Binding Highlights}" IsItemClickEnabled="True" SelectionMode="None" Width="312">
                                <GridView.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="10" Width="80">
                                            <Ellipse Width="70" Height="70" Stroke="Gray" StrokeThickness="2">
                                                <Ellipse.Fill>
                                                    <ImageBrush ImageSource="{Binding CoverUrl}" Stretch="UniformToFill"/>
                                                </Ellipse.Fill>
                                            </Ellipse>
                                            <TextBlock Text="{Binding Title}" FontSize="12" TextWrapping="Wrap" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </GridView.ItemTemplate>
                                <GridView.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <ItemsWrapGrid Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </GridView.ItemsPanel>
                            </GridView>
                        </ScrollViewer>
                    </StackPanel>
                </DataTemplate>
            </HubSection>
            <!-- Publicaciones en cuadrícula -->
            <HubSection Header="Fotos">
                <DataTemplate>
                    <GridView ItemsSource="{Binding Posts}" IsItemClickEnabled="True" SelectionMode="None">
                        <GridView.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="{ThemeResource SystemControlAccentDark2AcrylicWindowAccentDark2MediumHighBrush}" BorderThickness="1" Margin="1">
                                    <Image Source="{Binding ImagePath}" Stretch="UniformToFill" Width="120" Height="120"/>
                                </Border>
                            </DataTemplate>
                        </GridView.ItemTemplate>
                        <GridView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <ItemsWrapGrid Orientation="Horizontal" MaximumRowsOrColumns="3"/>
                            </ItemsPanelTemplate>
                        </GridView.ItemsPanel>
                    </GridView>
                </DataTemplate>
            </HubSection>
        </Hub>

    </Grid>
</Page>
